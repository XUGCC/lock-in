# Dependencies
node_modules/

# OS / editor
.DS_Store
Thumbs.db

# Capacitor generated config artifacts at root (platform copies live under android/ which has its own .gitignore)
android/.gradle/
android/local.properties
android/app/build/
android/build/
# Android Studio 本地 IDE 配置（每台机器不同，不纳入版本控制）
android/.idea/

# Signing — never commit keystores or passwords
*.jks
*.keystore
key.properties